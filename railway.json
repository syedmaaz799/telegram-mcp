{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "telegram-mcp --transport streamable-http --host 0.0.0.0",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 300
  }
}
